# 协作约定（Codex 执行规范）

本文件用于固化本项目的人机协作规则，确保后续会话可以快速恢复一致的工作方式。

## 项目目的
本项目用于本地维护个人GitHub 主页。


## 基本环境

- 代码编辑工具：`VSCode`
- 项目根目录：`/home/eda/my_lab/projects/Snappersontheprowl.github.io`

## Git 与提交流程

- 代码发生修改后，需及时进行本地 `git commit`。
- 不得擅自回滚或覆盖用户已有未授权变更。

## 重构与代码调整权限

- 涉及代码修改或重构时，可为提升整体清晰性进行必要的：
- 文件/文件夹新增
- 文件/文件夹删除
- 文件/文件夹重命名
- 坚决抵制任何兼容，只允许成功或者 git 回滚。
- 若属于“大改”或“重构级变更”，必须先在项目根目录 `TODO` 中写出分步骤详细方案，再开始实施。

## TODO 维护规则

- 每次“大改/重构级变更”完成后，必须在同一轮及时梳理根 `TODO`。
- 已完成事项应从根 `TODO` 及时收口删除，避免根 `TODO` 堆积完成项。
- 根 `TODO` 仅保留：未完成任务、暂缓池、监控项、归档索引。


## 文档同步

- 涉及行为、结构、入口、配置、流程变化时，需及时更新相关 `README` 和文档。

## 命名约定（新增）

- 所有正式命名默认使用稳定业务语义，不使用 `new`、`final`、`tmp`、`test2`、`v2` 这类阶段性词。
- 目录名、文件名、配置名应尽量让人只看名字就能判断职责，不依赖上下文猜测。
- 对于同一类或者同一性质的文件夹或者文件，命名请保持一定的规律。
- 若某类对象存在目录级命名规则，必须写在对应目录的 `README.md` 中；`AGENTS.md` 只保留全局原则。
- 涉及命名规则变化时，需同步更新对应目录 `README.md`，不能只改代码或只改配置。
